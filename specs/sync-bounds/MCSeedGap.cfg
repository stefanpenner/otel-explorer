\* Positive check of the spec's core question: with the window-slide fixed
\* (FreshNowAtListing=FALSE — now the SHIPPED behavior, fixed 2026-07-06)
\* and the seeder ACTIVE, seeded synced=0 runs never clamp the listing
\* window into a permanent gap — the store.go:101-105 protection works.
\* Expect: GREEN. (Same bounds as MCMutation2 but slide fixed + seeding on.)
CONSTANTS
    Runs = {r1, r2, r3}
    Syncers = {s1}
    MaxTime = 3
    MaxAttempt = 2
    Days = 3
    Overlap = 1
    Seeding = TRUE
    FreshNowAtListing = FALSE
    Bug = FALSE
    Bug2 = FALSE
SPECIFICATION Spec
INVARIANT TypeInvariant
INVARIANT NoPermanentGap
