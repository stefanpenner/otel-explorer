\* Fixed code WITH the hostile mutual-parent pair (was Finding 2 /
\* FINDINGS.md #11): parent-cycle containment (tree.go:277-316) must keep
\* every emitted span reachable. Expected GREEN.
\* FixOrder=TRUE: N=9 makes full order-permutation intractable; dedup order
\* coverage lives in MC.cfg. ChildrenLinkWhenParentIdSurvives is omitted:
\* a cycle-promoted node is its documented exception (edge cut on purpose).
SPECIFICATION Spec
CONSTANTS
  Bug          = FALSE
  Bug2         = FALSE
  Bug3         = FALSE
  IncludeCycle = TRUE
  FixOrder     = TRUE
INVARIANTS
  TypeOK
  PipelineMonotone
  Accounting
  NoSilentLossForest
  DedupIdempotent
  DedupCollapsesRunnerPair
  DedupKeepsAmbiguousGroup
  SelfParentIsRoot
  ZeroIdNeverParent
