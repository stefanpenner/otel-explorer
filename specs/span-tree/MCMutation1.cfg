\* Mutation for fixed Finding 1 (FINDINGS.md #10): Bug2=TRUE restores the
\* pre-2026-07-06 arrival-order dedup. Witness: [api, api', runner] sharing
\* one span ID — first pass keeps {runner, api'}, second pass drops api'.
\* MUST FAIL on DedupIdempotent (proves the invariant keeps its teeth).
\* (Was MCFinding1.cfg while the bug was live in the code.)
SPECIFICATION Spec
CONSTANTS
  Bug          = FALSE
  Bug2         = TRUE
  Bug3         = FALSE
  IncludeCycle = FALSE
  FixOrder     = TRUE
INVARIANTS
  TypeOK
  DedupIdempotent
