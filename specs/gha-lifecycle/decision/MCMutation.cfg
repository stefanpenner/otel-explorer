\* Mutation: Bug=TRUE drops !isPending on fail + queue.
\* PendingNeverFailed MUST FAIL (pending + conclusion=failure counted failed).
CONSTANTS
  Bug = TRUE
SPECIFICATION Spec
CHECK_DEADLOCK FALSE
INVARIANT TypeOK
INVARIANT PendingNeverFailed
