\* Mutation 4 (MUST FAIL) — restores Finding 4's pre-fix bug.
\* BugBounds = TRUE: pre-fix bounds only healed a MISSING UpdatedAt, so an
\* inverted UpdatedAt (< CreatedAt) with no completed job to extend it leaves
\* runEnd below runStart.
\* RunBoundsOrdered is GREEN under the fix (MC.cfg) and MUST FAIL here.
CONSTANTS
    Tmax = 4
    Threshold = 2
    Bug = FALSE
    BugClamp = FALSE
    BugBounds = TRUE
    BugSanity = FALSE
INIT Init
NEXT Next
INVARIANT TypeInvariant
INVARIANT RunBoundsOrdered
