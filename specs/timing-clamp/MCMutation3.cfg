\* Mutation 3 (MUST FAIL) — restores Finding 3's pre-fix bug.
\* BugClamp = TRUE: the pre-#8 ms/flamechart path used the RAW job start and
\* a +1-floor end, escaping the run window unboundedly.
\* MsChildWithinParent is GREEN under the fix (MC.cfg) and MUST FAIL here.
CONSTANTS
    Tmax = 4
    Threshold = 2
    Bug = FALSE
    BugClamp = TRUE
    BugBounds = FALSE
    BugSanity = FALSE
INIT Init
NEXT Next
INVARIANT TypeInvariant
INVARIANT MsChildWithinParent
