\* Mutation 1 (MUST FAIL) — restores Finding 1's pre-fix bug.
\* BugClamp = TRUE: pre-#8 clamp put the child span AT the parent's end;
\* the perfetto 1ms minimum (perfetto.go:51-52) then bumped it one past.
\* ExportedChildEndInParent is GREEN under the fix (MC.cfg) and MUST FAIL here.
CONSTANTS
    Tmax = 4
    Threshold = 2
    Bug = FALSE
    BugClamp = TRUE
    BugBounds = FALSE
    BugSanity = FALSE
INIT Init
NEXT Next
INVARIANT TypeInvariant
INVARIANT ExportedChildEndInParent
