\* Mutation 2 (MUST FAIL) — restores Finding 2's pre-fix bug.
\* BugClamp = TRUE: the pre-#8 clamp only bounded the end side, so a child
\* whose start precedes its parent's start survives (no start >= parentStart).
\* ExportedChildStartInParent is GREEN under the fix (MC.cfg) and MUST FAIL here.
CONSTANTS
    Tmax = 4
    Threshold = 2
    Bug = FALSE
    BugClamp = TRUE
    BugBounds = FALSE
    BugSanity = FALSE
INIT Init
NEXT Next
INVARIANT TypeInvariant
INVARIANT ExportedChildStartInParent
