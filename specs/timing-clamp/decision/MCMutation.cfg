\* Mutation: Bug = TRUE → raw passthrough, no parent clamp.
\* ClampedContained MUST FAIL (proves the invariant has teeth).
CONSTANTS
    Tmax = 4
    Bug = TRUE
INIT Init
NEXT Next
INVARIANT ClampedContained
