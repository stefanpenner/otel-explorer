\* MCBait.cfg — bait: asserts log spans are never displayed.  That state
\* is clearly reachable (press L, fetch delivers).  MUST FAIL with a
\* witness trace — proves TLC is actually exploring fetch delivery.
CONSTANTS
  Jobs = {j1, j2}
  MaxGen = 2
  Bug = FALSE
  Bug2 = FALSE
SPECIFICATION Spec
CHECK_DEADLOCK FALSE
INVARIANT TypeInvariant
INVARIANT BaitNoLogsDisplayed
