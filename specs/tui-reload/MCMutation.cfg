\* MCMutation.cfg — faithful fetch handling (Bug2=FALSE) but Bug=TRUE
\* removes the isLoading key guard (model.go:473-479): a second reload
\* can start while one is in flight.  Expected: FAIL
\* (AtMostOneReloadRunning first).  Proves the invariants have teeth.
CONSTANTS
  Jobs = {j1, j2}
  MaxGen = 2
  Bug = TRUE
  Bug2 = FALSE
SPECIFICATION Spec
CHECK_DEADLOCK FALSE
INVARIANT TypeInvariant
INVARIANT AtMostOneReloadRunning
INVARIANT NoStaleLogSpans
INVARIANT FetchedJobsHaveTheirSpans
INVARIANT DisplayedIsLatestSuccess
INVARIANT NoStuckListen
