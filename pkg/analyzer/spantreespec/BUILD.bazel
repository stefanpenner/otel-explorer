load("@rules_go//go:def.bzl", "go_library", "go_test")

# Generated by specgen from specs/span-tree/decision/Decision.tla
# (logical name: SpanTreeDecision).
# Never hand-edit spec.go / spec_test.go — regenerate:
#   specgen -const Bug=FALSE \
#     -o pkg/analyzer/spantreespec -p spantreespec \
#     specs/span-tree/decision/Decision.tla

go_library(
    name = "spantreespec",
    srcs = ["spec.go"],
    importpath = "github.com/stefanpenner/otel-explorer/pkg/analyzer/spantreespec",
    visibility = ["//visibility:public"],
)

go_test(
    name = "spantreespec_test",
    srcs = ["spec_test.go"],
    embed = [":spantreespec"],
)
