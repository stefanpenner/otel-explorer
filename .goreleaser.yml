version: 2

builds:
  - id: ote
    main: ./cmd/ote
    binary: ote
    env:
      - CGO_ENABLED=0
    goos:
      - linux
      - darwin
    goarch:
      - amd64
      - arm64
    ldflags:
      - -s -w

archives:
  - id: ote
    builds: [ote]
    format: tar.gz
    name_template: "{{ .ProjectName }}_{{ .Os }}_{{ .Arch }}"
    files:
      - none*

checksum:
  name_template: "checksums.txt"
  algorithm: sha256

release:
  github:
    owner: stefanpenner
    name: otel-explorer
  draft: false
  prerelease: auto

changelog:
  sort: asc
  filters:
    exclude:
      - "^docs:"
      - "^test:"
      - "^chore:"
      - Merge pull request
      - Merge branch

brews:
  - name: ote
    repository:
      owner: stefanpenner
      name: homebrew-tap
      token: "{{ .Env.HOMEBREW_TAP_GITHUB_TOKEN }}"
    homepage: "https://github.com/stefanpenner/otel-explorer"
    description: "Analyze OpenTelemetry traces to identify performance bottlenecks"
    license: ""
    install: |
      bin.install "ote"
      bin.install_symlink "ote" => "otel-explorer"
    test: |
      system "#{bin}/ote", "--help"
